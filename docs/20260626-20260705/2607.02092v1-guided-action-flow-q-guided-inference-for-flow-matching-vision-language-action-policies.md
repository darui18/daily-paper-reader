---
title: "Guided Action Flow: Q-Guided Inference for Flow-Matching Vision-Language-Action Policies"
title_zh: 引导动作流：面向流匹配视觉-语言-动作策略的Q引导推理
authors: "Liuhaichen Yang, Zhuang Jiang, Chenchao Sheng, Zezhi Tang"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02092v1"
tags: ["query:diffusion-policy"]
score: 9.0
evidence: 基于Q引导推理的流匹配VLA策略
tldr: "本文提出Guided Action Flow，一种基于流匹配的视觉-语言-动作策略的推理时引导框架，无需重新训练基础策略。通过保持预训练SmolVLA策略冻结，并利用成功与失败轨迹训练的动作块评判器，在反向流采样过程中以动作梯度引导生成，提升机器人操作成功率。在LIBERO任务上，单任务评判器将成功率从68.0%提升至82.0%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02092v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1774, \"height\": 1009, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02092v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 939, \"height\": 230, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02092v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 821, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02092v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 796, \"height\": 163, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02092v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 711, \"height\": 386, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02092v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1524, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02092v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 792, \"height\": 163, \"label\": \"Table\"}]"
problem_solution: 现有流匹配视觉-语言-动作策略缺乏测试时引导能力，且重训练成本高。本文通过保持基础策略冻结，训练一个动作块评判器，利用其梯度在反向流采样中对动作轨迹进行引导，从而在不更新策略参数的情况下提升任务成功率。
---

## 摘要
流匹配视觉-语言-动作策略通过迭代传输过程生成机器人动作片段，为在不重新训练基础策略的情况下进行测试时引导提供了机会。我们在引导动作流中研究了这一机会，这是一个推理时框架，保持预训练的SmolVLA策略冻结，并使用学习到的动作片段评判器来引导其反向时间流采样器。该评判器通过真实的成功与失败轨迹训练，可以根据冻结的SmolVLA语言路径中的任务描述特征进行条件设置，并且仅在采样过程中通过动作梯度使用。我们在LIBERO操作任务上评估了该方法。一个单任务评判器将一个种子窗口的成功率从68.0%提高到82.0%，另一个窗口从82.0%提高到86.0%。一个多系列任务描述评判器将验证成功率从46.0%提高到56.0%，而锁定的保留测试提升幅度虽小但为正向，从65.0%到67.5%。这些结果支持了针对冻结的流匹配VLA策略的Q引导推理的可行性，同时表明评判器的泛化和不确定性感知引导仍然是主要瓶颈。

## Abstract
Flow-matching vision-language-action policies generate robot action chunks through an iterative transport process, creating an opportunity for test-time guidance without retraining the base policy. We study this opportunity in Guided Action Flow, an inference-time framework that keeps a pretrained SmolVLA policy frozen and uses a learned action-chunk critic to guide its reverse-time flow sampler. The critic is trained from real success and failure rollouts, can condition on task-description features from the frozen SmolVLA language pathway, and is used only through action gradients during sampling. We evaluate the approach on LIBERO manipulation tasks. A single-task critic improves success from 68.0% to 82.0% on one seed window and from 82.0% to 86.0% on another. A multi-family task-description critic improves validation success from 46.0% to 56.0%, while the locked held-out test gain is positive but modest, from 65.0% to 67.5%. These results support the feasibility of Q-guided inference for frozen flow-matching VLA policies, while showing that critic generalization and uncertainty-aware guidance remain the central bottlenecks.

---

## 论文详细总结（自动生成）

## 0. 为什么推荐精读
这篇论文提出了一种“测试时引导”的轻量级框架，在不重新训练基础策略的前提下，仅通过一个外挂评判器的梯度就能提升冻结的流匹配VLA（视觉-语言-动作）策略的性能。方法具备极强的模块化和实用性，实验清晰区分了可行性与瓶颈，对研究VLA模型适应、离线强化学习、生成式机器人策略的学者有重要参考价值。此外，论文诚实地报告了负面结果（空间仅评判器失效、保留测试提升微弱），这种严谨的学术态度值得借鉴。

## 1. 论文简介和核心思想
Guided Action Flow 针对流匹配VLA策略（以SmolVLA为例）提出了一种推理时引导方法：保持基础策略完全冻结，从策略的真实rollout中训练一个动作片段（action-chunk）评判器（critic）；在反向流采样过程中，计算评判器相对于预估干净动作的梯度，并用该梯度修正流速度场，从而引导生成更有利于任务完成的动作序列。核心思想是不修改策略参数，仅利用评判器信号重塑采样轨迹，实现低成本、模块化的策略适应。

## 2. 论文发现的问题
- 预训练VLA策略虽有通用能力，但在特定任务上仍存在失败，且失败通常源于分布偏移、早期动作累积误差或局部最优动作选择。
- 全参数微调VLA成本高昂，对硬件和验证要求高，尤其在小规模消费级GPU场景下不实用。
- 流匹配VLA的迭代生成过程提供了一个自然的外部引导接口，但现有工作（如QGF）主要面向标准RL，未在冻结VLA上进行验证，且冻结策略的采样约定（反向时间流）需要正确推导引导符号。

## 3. 解决方案
**整体框架**（图1）：
1. **数据收集**：用冻结的SmolVLA策略在LIBERO环境中执行rollout，收集状态、动作片段、成功标志。
2. **评判器训练**：以起点观测特征+动作片段+可选的SmolVLA语言隐层特征为输入，MLP回归稀疏成功-到-去（success-to-go）目标。目标定义为距离最近成功步的折扣指数衰减。采用episode级划分训练/验证集。
3. **推理时引导**：
   - 在每个反向去噪步，用当前样本 `x_t` 和模型预测速度 `v_t` 估计干净动作 `ˆa = x_t - t v_t`。
   - 计算K个评判器的平均值 `¯Q(ˆa)` 对 `ˆa` 的梯度 `g`。
   - 对梯度进行裁剪和门控（门控系数 `m = max(m_min, exp(-α σ_Q))`，其中 `σ_Q` 是评判器集成标准差）。
   - 修正速度：`v_guided = v_t - m·clip(g, c)/β`（负号源于反向时间约定）。
   - 用修正后的速度执行标准采样积分。
4. **不确定性缓解**：集成门控在评判器间分歧大时降低引导强度，防止“OOD”梯度破坏行为。

**关键设计细节**：评判器仅对LIBERO物理动作维度应用，填充维度不变；任务特征使用冻结VLM隐层池化，比任务ID更可转移；基础策略所有参数冻结，梯度不反向传播进入VLA。

## 4. 效果
**实验设置**：LIBERO标准套件，SmolVLA官方冻结checkpoint。分三种协议：单任务、空间仅转移、多系列（含空间与物体系列）验证+保留测试。

**基线锚点**：冻结SmolVLA在LIBERO上65/100 (65.0%)，LIBERO-PRO仅1%零样本，适合当前分析。

**主要结果**（表Ⅲ/Ⅳ/Ⅵ）：
- **单任务**：两个种子窗口分别从68.0%→82.0% (+14.0 pp) 和 82.0%→86.0% (+4.0 pp)，证明引导可以切实改善闭环表现。
- **空间仅多任务评判器**：验证分割上基线32/60，QGF最多31/60（负面结果），说明仅增加同一系列任务数不足以泛化。
- **多系列任务描述评判器**（含250空间+250物体rollout）：验证集从46.0%→56.0% (+10.0 pp)；保留测试（未见任务）从65.0%→67.5% (+2.5 pp)。验证提升显著，保留测试提升微弱但正向，表明评判器泛化仍是主要瓶颈。

**消融/敏感性**：β、梯度裁剪范数、门控系数、集成大小均敏感，论文在验证集上选参，保留测试锁住，保证可信度。

## 5. 下一步计划
- **扩充评判器数据**：覆盖更多LIBERO系列、更大保留测试预算，评估统计显著性。
- **改进评判器目标**：引入成对排序损失、保守值正则化，替代简单的均方误差回归，以提供更优的排序信号。
- **增强视觉表征**：使用更丰富的策略侧视觉特征（如预训练视觉编码器）替代紧凑特征，提升值估计质量。
- **OOD感知引导**：除集成分歧外，融合任务嵌入距离、最近邻距离或学习OOD检测器，在离群点时完全禁用引导。
- **真实机器人实验**：在物理机器人上验证，考虑传感器噪声、执行延迟和恢复行为对引导有效性的影响。
- **将本框架融入残差学习线**：类似残差强化学习思想，但本方法在动作生成轨迹内部做梯度修正，可与离线RL结合。

（完）
